document.addEventListener('DOMContentLoaded', function () {
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const mobileMenu = document.getElementById('mobile-menu');
    const hamburgerIcon = document.getElementById('hamburger-icon');
    const closeIcon = document.getElementById('close-icon');

    if (mobileMenuButton && mobileMenu && hamburgerIcon && closeIcon) {
        mobileMenuButton.addEventListener('click', function () {
            const isOpen = mobileMenu.classList.contains('menu-open');
            if (isOpen) {
                mobileMenu.classList.remove('menu-open');
                hamburgerIcon.classList.remove('hidden');
                closeIcon.classList.add('hidden');
                mobileMenuButton.setAttribute('aria-expanded', 'false');
            } else {
                mobileMenu.classList.add('menu-open');
                hamburgerIcon.classList.add('hidden');
                closeIcon.classList.remove('hidden');
                mobileMenuButton.setAttribute('aria-expanded', 'true');
            }
        });
    }

    // Language switcher (if any, logic would go here)
    // For example:
    // const langSwitchButton = document.getElementById('lang-switch');
    // if(langSwitchButton) {
    //     langSwitchButton.addEventListener('click', function() {
    //         const currentLang = document.documentElement.lang;
    //         if (currentLang === 'ar') {
    //             // Switch to English logic
    //             // window.location.href = window.location.href.replace('/ar/', '/en/'); // Example
    //             console.log('Switching to English');
    //         } else {
    //             // Switch to Arabic logic
    //             // window.location.href = window.location.href.replace('/en/', '/ar/'); // Example
    //             console.log('Switching to Arabic');
    //         }
    //     });
    // }
});
